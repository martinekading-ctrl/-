# 上线就绪与安全边界

## 当前产品定位

Multi-Chain Token Radar 是一个**新池研究与本地模拟交易工具**。它发现已进入公共 RPC / DEX 聚合数据源的 EVM 新池，记录可观察的安全、流动性与早期成交证据，并以固定策略进行纸面验证。

它不是收益承诺，也不是“首区块必成交”的链上狙击器。公共 RPC、第三方安全接口和 DEX 索引均存在延迟、限流、缺失与重组风险；软件会展示每轮实际耗时，而不会把“5 秒后开始下一轮”描述为 5 秒链上发现。

当前版本不连接钱包、不保管私钥、不签名，也不会发送真实交易。

## 已实现的保护

- 严格候选缺失关键安全字段时直接拒绝；严格模式要求 LP 锁定、集中度和高风险合约项均通过。
- 新池年龄采用区块时间而非 24 小时列表排序；历史池被年龄门槛剔除。
- 安全数据和市场数据分别退避，短暂 429 / 5xx 不会造成无休止请求或把未知状态误标为安全。
- 模拟开仓保存精确池地址；报价丢失会暂停自动平仓/开仓，而不是以零价格结算。
- 模拟滑点按流动性估算；超过策略上限的开仓直接拒绝，无法承受的卖出保守记为不可执行。
- 每次策略参数改变都会产生新的策略指纹，历史样本不与新策略混合回测。
- 更新包下载后校验 SHA-256，Windows 替换前会保留 `.preupdate.bak`，并对短暂文件锁重试和失败回退。

## 仍然不能被软件“保证”的事情

- 任何信号都不能证明代币有价值，更不能保证盈利。动能分数只归纳已观察到的 5 分钟成交、买卖方向、成交额/流动性和价格变化。
- GoPlus、DexScreener、公共 RPC 的字段不完整或服务限流时，结果只能是“未知/等待”，不能推断为安全。
- 当前 HTTP 轮询不能替代专用节点 WebSocket、mempool 或交易所级低延迟基础设施；免费来源也不适合承诺发现延迟。
- 校验和可排除传输损坏，但如果发布仓库本身被入侵，校验文件也可能被替换。正式生产发布必须加入离线 Ed25519 签名、固定公钥验证和发布审批。

## 允许进入真实资金前的硬性验收门槛

以下项目必须全部完成，并有可复核记录；缺一项都不应打开真实下单：

1. **数据质量**：每条链至少连续 30 天记录数据源可用率、扫描实际耗时、区块滞后、字段缺失率、退避次数和去重准确率；错误或缺失须失败关闭。
2. **策略验证**：至少 100 个同一策略指纹、同一候选规则下的完整模拟结果；按时间切分训练/样本外区间，结果包含滑点、费用、无法退出案例和最大回撤，不能只展示胜率或单笔收益。
3. **对抗测试**：用 honeypot、可改税、暂停交易、黑名单、LP 撤出、假流动性、极端滑点和报价中断样本进行回归测试，任何无法确认的安全项默认拒绝。
4. **执行演练**：先在独立测试网和金额上限极低的隔离环境中演练；必须支持幂等订单、nonce 管理、成交回报核对、超时取消、链重组处理和紧急总开关。
5. **密钥与权限**：独立热钱包、最小余额、硬件或受管密钥、绝不把助记词/私钥写入程序或配置文件；交易合约白名单、单笔/每日损失上限和人工撤销必须可用。
6. **独立审查**：对交易构造、更新器、密钥存储、依赖项和部署配置完成独立安全审计；高危问题归零后再复测。
7. **发布治理**：发布包用离线私钥签名，客户端固定公钥校验；发布前生成 SBOM、可复现构建哈希、回滚包和人工批准记录。

## 建议的真实化顺序

1. 用当前版本采集并导出真实观察数据，先验证“发现质量”而非交易结果。
2. 在不改变策略指纹的条件下完成足量纸面样本，明确样本外表现和最坏情形。
3. 接入有 SLA 的专用节点、链上事件订阅和可审计的数据存储，先只报警，不交易。
4. 建立测试网执行器和风险总开关，通过演练和独立审计。
5. 仅在再次明确授权后，另行设计真实交易模块；该模块应作为独立、可禁用的组件，而不是直接复用桌面界面的模拟逻辑。
